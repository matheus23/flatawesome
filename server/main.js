import { Meteor } from 'meteor/meteor'

import "../imports/api/ShoppingListCollection"
import "../imports/api/FinancesCollection"
import "../imports/api/FlatsCollection"

Meteor.startup(() => {
  // code to run on server at startup
})
